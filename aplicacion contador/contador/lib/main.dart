import 'package:flutter/material.dart';

//nuestros paquetes
import 'src/app.dart';

void main() {
  runApp(MyApp());
}

/**
 * void main es la funcion principal con la que se ejecuta nuesta applicacion
 * runApp() ejecuta nuestra applicacion
 * import 'package:flutter/material.dart'; paquetes de estilo de android / cupertino para ios
 * 
 * las primeras importaciones deben ser aquellas que no escribimos despues las hechas por nosotros
 */

